# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CanDraw' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CanDraw

  target 'CanDrawTests' do
    inherit! :search_paths
    pod 'Nimble'
    pod 'Quick'
    pod 'Nimble-Snapshots'
  end

end
